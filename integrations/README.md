# Integrations

This directory groups editor and tool integrations for ByteORM.

- `intellij/byteorm-intellij/` holds the IntelliJ Platform MVP for `.bo` schema support.
- Additional editors can follow the same layout without changing the Rust workspace.

